1.virtualenv venv
2.source venv/bin/activate
install from requiremnts.txt
/package will give packages
3. pip install -r requirements.txt
4. uvicorn main:app --reload --port 8001

install from requiremnts.txt
/package will give packages
